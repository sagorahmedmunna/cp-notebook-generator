# latexmk configuration file
# This allows users to simply run: latexmk main.tex

$pdf_mode = 1;
$pdflatex = 'pdflatex -shell-escape -interaction=nonstopmode %O %S';
$postscript_mode = 0;
$dvi_mode = 0;
